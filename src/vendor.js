const dashboard = document.getElementById('dashboard');
const botsDashboard = document.getElementById('bots-dashboard');
const otherDashboard = document.getElementById('other-dashboard');

const mqttSettingsApplication = document.getElementById('mqtt-settings-application');
const botListApplication = document.getElementById('bot-list');
const addBotApplicationBtn = document.getElementById('add-bot-application');
const removeBotApplication = document.getElementById('remove-bot-application');

const debugCommandCentral = document.getElementById('debug-item');
